-- Phase 2 implementation target: paginated transactions query
SELECT
  CAST(NULL AS STRING) AS order_id,
  CAST(NULL AS DATE) AS order_date,
  CAST(NULL AS STRING) AS region,
  CAST(NULL AS STRING) AS product_name,
  CAST(0 AS INT64) AS quantity,
  CAST(0 AS NUMERIC) AS net_sales
WHERE 1 = 0;

